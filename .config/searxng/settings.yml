use_default_settings: true

search:
  formats:
    - html
    - json

server:
  secret_key: "llama-cli-searxng-local"
  # Limiter disabled: local-only instance, no public exposure (ADR-057)
  limiter: false
